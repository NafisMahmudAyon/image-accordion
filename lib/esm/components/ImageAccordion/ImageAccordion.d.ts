import React, { ReactElement } from 'react';
interface ImageAccordionProps {
    styles?: string;
    children: ReactElement | ReactElement[];
}
export declare const ImageAccordion: React.FC<ImageAccordionProps>;
export {};
